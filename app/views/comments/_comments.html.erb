<p>Comments</p>
<% if @users.commentable.comments.present? %>
    <% @users.commentable.comments.each do |comment| %>
        <div>
            <p><strong><%= comment.first_name %> </strong> <br>
            <%= comment.description %></p>
        </div>
    <% end %>
<% end %>